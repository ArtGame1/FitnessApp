<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Дневная тема -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/menuColor</item>
        <item name="colorPrimaryDark">@color/menuColor</item> <!-- ИЗМЕНИЛ НА menuColor -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Ночная тема -->
    <style name="AppTheme.Night" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/menuColor</item> <!-- ТОЖЕ ИЗМЕНИЛ -->
        <item name="colorPrimaryDark">@color/menuColor</item> <!-- ТОЖЕ ИЗМЕНИЛ -->
        <item name="colorAccent">@color/colorAccentNight</item>
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/gray_light</item>
    </style>
</resources>